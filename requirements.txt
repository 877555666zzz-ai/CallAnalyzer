# ── Зависимости для деплоя (Railway): дашборд + боевой прогон на route ──
# STT работает по HTTP (Deepgram + ElevenLabs), поэтому torch/whisper НЕ нужны.

# ядро
anthropic>=0.40
pyyaml>=6.0
jsonschema>=4.0
sqlalchemy>=2.0
requests>=2.31

# альтернативный LLM для анализа (Gemini/Grok через OpenAI-совместимый SDK)
openai>=1.40

# веб-дашборд
fastapi>=0.110
starlette>=0.36
uvicorn>=0.29
jinja2>=3.1
python-multipart>=0.0.9   # request.form() — нужен вебхуку Kcell (application/x-www-form-urlencoded)

# PostgreSQL на Railway
psycopg[binary]>=3.1

# ── Whisper (self-host) НЕ ставим на Railway — тянет torch/CUDA (~2 ГБ) и не нужен
# при STT_MODE=route. Если когда-нибудь понадобится STT_MODE=whisper на отдельном
# GPU-воркере, туда добавить: faster-whisper>=1.0  и  soundfile>=0.12